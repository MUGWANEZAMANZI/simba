# TODO

## Background video (Pexels/YouTube) for Welcome/hero
- [ ] Add background video element to the hero/search section in `src/App.jsx` (view === "home" && selectedBranch exists).
- [ ] Add CSS in `src/styles.css` for full-bleed background video + readable gradient overlay.
- [ ] Ensure the overlay does not break layout (z-index layering) and remains responsive.
- [ ] Add reduced-motion / mobile fallback so performance stays good.
- [ ] Run the app to visually confirm the video background works on the hero/search section.

